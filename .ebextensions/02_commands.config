commands:
  01_verify_docker:
    command: docker --version
  02_verify_mysql_connectivity:
    command: |
      echo "Connectivity check will be done during application initialization"

container_commands:
  01_create_log_dirs:
    command: mkdir -p /var/log/bank-management || true
